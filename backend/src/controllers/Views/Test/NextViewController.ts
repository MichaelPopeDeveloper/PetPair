
export default class NextViewController {
  private nextApp: any;

  public static CheckIfSessionActive(req: any, res: any, next: any) {
    // const actualPage = '/post';
    // this.nextApp.render(req, res, actualPage);
  }
}
